# Development & CI dependencies.
# Installs everything from the production file plus tooling for tests/lint/security.
# Usage: pip install -r requirements-dev.txt
-r requirements.txt

# Test runner (moved here out of the production requirements.txt).
pytest==9.0.3

# Linter / import sorter.
ruff>=0.15.17,<1.0

# Security static analysis.
bandit>=1.9.4,<2.0

# Dependency vulnerability audit (CI: advisory via continue-on-error).
pip-audit>=2.10.1,<3.0
